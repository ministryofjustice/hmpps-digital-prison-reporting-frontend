{% from "govuk/components/table/macro.njk" import govukTable %}
{% from "../search/view.njk" import dprSearch %}

{% macro dprReportsList(head, rows) %}

  {% set tableHtml %}
  {{ govukTable({
    captionClasses: "govuk-table__caption--m",
    head: head,
    rows: rows,
    classes: "dpr-search-table"
  }) }}
  {% endset %}

  {{ dprSearch(tableHtml) }}

{% endmacro %}
