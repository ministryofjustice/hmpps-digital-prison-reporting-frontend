export interface BookmarkedReportData {
  reportId: string
  variantId: string
  reportName: string
  name: string
  description: string
  href: string
}

export interface BookmarkStoreData {
  reportId: string
  variantId: string
}
