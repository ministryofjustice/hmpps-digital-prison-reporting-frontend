import DashboardUtils from './dashboardUtils'

describe('DashboardService', () => {
  beforeEach(() => {
    //
  })

  describe('getDashboardData', () => {
    it('should...', async () => {
      expect(true)
    })
  })
})
