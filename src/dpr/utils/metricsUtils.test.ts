import MetricsUtils from './metricsUtils'

describe('DashboardService', () => {
  beforeEach(() => {
    //
  })

  describe('getMetricData', () => {
    it('should...', async () => {
      expect(true)
    })
  })
})
