const { fallBackKeysDashboard } = require('./lists-fallback-keys')
const { dietTotals } = require('./lists-diet-totals')
const { historicDietTotals } = require('./lists-historic-diet-totals')

module.exports = {
  fallBackKeysDashboard,
  dietTotals,
  historicDietTotals,
}
