const listSection = require('./list-section')
const listSectionWithSummaries = require('./list-section-wtih-summaries')
const listWithSummaries = require('./list-with-summaries')
const parentChild = require('./parent-child')
const parentChildSection = require('./parent-child-section')
const rowSection = require('./row-section')
const rowSectionChild = require('./row-section-child')
const rowSectionChildMultiple = require('./row-section-child-multiple')
const rowSectionMultiple = require('./row-section_multiple-rows')
const sectionedSummaries = require('./sectioned-summaries-mock')
const summariesComplex = require('./summaries-complex')
const summariesMock = require('./summaries-mock')
const summary = require('./summary')
const summarySection = require('./summary-section')
const tableSummaries = require('./table-summaries')

module.exports = [
  listSection,
  listSectionWithSummaries,
  listWithSummaries,
  parentChild,
  parentChildSection,
  rowSection,
  rowSectionChild,
  rowSectionChildMultiple,
  rowSectionMultiple,
  sectionedSummaries,
  summariesComplex,
  summariesMock,
  summary,
  summarySection,
  tableSummaries,
]
